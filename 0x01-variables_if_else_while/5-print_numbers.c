#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more header gets there */

/* betty style doc for function main gets there */
/**
 * main - Entry
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
